include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Personalizar reglas específicas si es necesario
    lines_longer_than_80_chars: false
    public_member_api_docs: true

analyzer:
  exclude:
    - '**/*.g.dart'
    - '**/*.freezed.dart'
    - 'build/**'